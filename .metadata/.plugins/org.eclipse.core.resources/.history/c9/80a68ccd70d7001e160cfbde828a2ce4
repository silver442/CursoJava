package hernandez.silver.ejerciciospoo;

public class Colegio {
	
	public Colegio(String nombreColegio, int cantidadAlumnos) {
		this.nombreColegio=nombreColegio;
		
		this.cantidadAlumnos=cantidadAlumnos;
		
		nameAlumno=null;
	}
	
	public void nuevoAlumno(String nombreAlumno, double nota) {
		
		nameAlumno=nombreAlumno;
		
		numAlumno=alumnoSiguiente;
		alumnoSiguiente++;
		
		alumnos=new Alumno[cantidadAlumnos];
		
		alumnos[posicionArray] = new Alumno(nameAlumno, nombreColegio, numAlumno ,nota);
		
		posicionArray++;
	}
	
	// Métodos getter
	
	public String getTodosAlumnos() {
		
		for(int i=1;i<alumnoSiguiente;i++) {
			System.out.println(alumnos[i].getDatosAlumno());
		}
		
		return "\n";
	}
	
	public String getDatoAlumno(String nombreAlumno) {
		
		String nombre;
		
		for(int i=1;i<alumnoSiguiente;i++) {
			
			nombre=alumnos[i].getAlumno();
			
			if(nombreAlumno.equals(nombre)) {
				System.out.println(alumnos[i].getDatosAlumno());
			}else
				System.out.println("EL alumno no se encontro");
		}
		
		return "\n";
	}
	
	
	private final String nombreColegio;

	private String nameAlumno;
	
	private int cantidadAlumnos;
	
	private static Alumno[] alumnos;
	
	private int numAlumno;
	
	private static int alumnoSiguiente=1;
	
	private int posicionArray=0;
}
